[project]
name = "agent"
version = "0.1.0"
description = "A minimal autonomous agent framework"
requires-python = ">=3.12"
dependencies = [
    "websockets>=14.0",
    "httpx>=0.28",
    "pydantic>=2.10",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agent"]
