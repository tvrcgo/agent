model:
  provider: openai
  name: deepseek-v4-pro
  base_url: https://api.deepseek.com
  api_key: ${DEEPSEEK_API_KEY}

agent:
  window_size: 50
  max_iterations: 100

ws:
  host: 0.0.0.0
  port: 8765

skills:
  modules:
    - agent.skills.echo

plugins:
  modules:
    - agent.plugins.session
